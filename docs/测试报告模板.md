# trading-order-service 测试报告（D1–D9）

> 填写说明：每条用例记录「期望 / 实际 / 结论」，失败用例必须写清复现步骤与修复动作。

## 一、测试环境

| 项 | 值 |
| --- | --- |
| 日期 |  |
| 代码版本（commit） |  |
| 操作系统 | Windows 11 |
| JDK | 17.0.12 |
| Maven | 3.9.16（C:\Users\xle\Tools\apache-maven-3.9.16\bin\mvn.cmd） |
| 依赖 | MySQL 8（localhost:3307）、Redis 7（localhost:6379）、Kafka 3.7（localhost:9092，topic trading.order.events） |
| 应用启动参数 | -Dfile.encoding=UTF-8 spring-boot:run（如需短超时：--app.stock.lock-timeout=PT10S --app.receipt.timeout=PT10S） |

## 二、测试结果汇总

| 层次 | 用例数 | 通过 | 失败 | 跳过 | 备注 |
| --- | --- | --- | --- | --- | --- |
| L1 单元测试（mvn clean test） | 55 |  |  |  |  |
| L2 端到端 e2e-test.ps1（A–G） | 14 |  |  |  |  |
| L3 并发 OrderConcurrencyLocalIT | 2 |  |  |  |  |
| L4 故障注入 chaos-test.ps1 | 4 |  |  |  |  |
| L5 一致性不变量 | 7 |  |  |  |  |

## 三、关键用例记录

| 用例 | 期望 | 实际 | 结论 |
| --- | --- | --- | --- |
| A1 健康检查 | HTTP 200，code=0 |  |  |
| B1 正常下单 | 200，status=CREATED |  |  |
| B2 缺幂等请求头 | 400 |  |  |
| B3 quantity=0 | 400 |  |  |
| B4 缺 userId | 400 |  |  |
| C1 同 key 重复 | 同一 orderNo |  |  |
| C2 幂等键唯一 | 库中 1 行 |  |  |
| D1 库存不足 | 422 |  |  |
| E1 Outbox 落库 | 1 条 PENDING |  |  |
| E2 Outbox 投递 | status=1 |  |  |
| F1 金额一致 | CONFIRMED |  |  |
| F2 重复回执 | t_receipt 1 行 |  |  |
| F3 金额不一致 | FAILED + AMOUNT_MISMATCH |  |  |
| F4 上游失败 | FAILED + UPSTREAM_FAILED |  |  |
| G1 不变量 | 7 项 violations=0 |  |  |

## 四、并发与性能

| 场景 | 并发 | 成功 | 失败 | 剩余库存 | QPS | P99(ms) | 是否超卖 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 不同幂等键打 100 库存 | 200 |  |  |  |  |  |  |
| 同一幂等键 | 200 |  |  |  |  |  |  |

## 五、故障注入

| 场景 | 操作 | 期望 | 实际 | 结论 |
| --- | --- | --- | --- | --- |
| Redis 宕机 | docker stop trading-redis | 下单成功；同 key 仍只落 1 单（DB 唯一索引兜底） |  |  |
| Kafka 宕机 | docker stop trading-kafka | 下单成功；outbox 保持 PENDING 且 retry_count 增长 |  |  |
| Kafka 恢复 | docker start trading-kafka | outbox 最终 SENT（至少一次 + 消费幂等） |  |  |
| MySQL 宕机 | docker stop trading-mysql | 下单失败，但 /api/health 仍 200 |  |  |
| 进程崩溃恢复 | kill 应用后重启 | 未发送 outbox 继续投递 |  |  |

## 六、缺陷列表

| # | 严重度 | 描述 | 复现步骤 | 状态 | 修复动作 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |

## 七、结论与遗留风险

- 结论：
- 未覆盖 / 待补：
  - 商品价格模型未接入（订单 amount 目前恒为 0.00，属 D10 范围）
  - 正式性能报告（JMeter）计划在 D12 完成
  - 多实例部署下的 outbox 扫描（SKIP LOCKED / 分片）未验证
